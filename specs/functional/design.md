# Design — API Live Override

Base: [`shared/DocApi/functional/design.md`](../../shared/DocApi/functional/design.md)

## Overrides

**Site name:** `"API Live | Reference"` — displayed in Inter 600 in the navbar, centered (overrides the base spec's "immediately after the logo" placement).

**Navbar title position:** The title is centered in the navbar using absolute positioning, rather than placed immediately after the logo as described in the base spec.
