# Deployment — API Live Override

Base: [`shared/DocApi/technical/deployment.md`](../../shared/DocApi/technical/deployment.md)

## Overrides

**Root redirect URL:** `/docs/introduction/` — the route in `staticwebapp.config.json` that sends `/` to the Introduction page.
